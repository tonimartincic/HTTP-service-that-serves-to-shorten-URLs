import React from 'react';
import {Row} from 'react-bootstrap';
import styles from './URLRegisterHeader.css';

const URLRegisterHeader = () => (
  <Row>
    <h1 className={styles.h1}>URL register</h1>
  </Row>
);

export default URLRegisterHeader;
