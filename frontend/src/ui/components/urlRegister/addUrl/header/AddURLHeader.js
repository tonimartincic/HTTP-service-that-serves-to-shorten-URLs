import React from 'react';
import {Row} from 'react-bootstrap';
import styles from './addURLHeader.css';

const AddURLHeader = () => (
  <Row>
    <h1 className={styles.h1}>URL register</h1>
  </Row>
);

export default AddURLHeader;
